services:
  # ═══════════════════════════════════════════════════════════════
  # STAGEZERO BETA/RELEASE BUILD
  # ═══════════════════════════════════════════════════════════════
  # This compose file is for beta testing with a production-like build
  # Uses release build target but with development certificates
  # ═══════════════════════════════════════════════════════════════
  beta-stagezero:
    container_name: beta-stagezero
    image: stagezero:beta
    build:
      context: .
      dockerfile: StageZero/Dockerfile
      target: release  # Production build for beta testing
    ports:
      - "5100:443"
    env_file:
      - .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=https://+:443
      - ASPNETCORE_Kestrel__Certificates__Default__Password=${CERT_PASSWORD}
      - ASPNETCORE_Kestrel__Certificates__Default__Path=/https/aspnetapp.pfx
      # Database path is now managed by DataPathService in the application
      # The application will use platform-specific paths automatically
    volumes:
      # Application data directory (database, logs, etc.)
      # Use STAGEZERO_DATA_DIR environment variable to override
      - ${STAGEZERO_DATA_DIR:-./.volumes/stagezero-data}:/app-data

      # HTTPS development certificate
      - ~/.aspnet/https:/https:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"


